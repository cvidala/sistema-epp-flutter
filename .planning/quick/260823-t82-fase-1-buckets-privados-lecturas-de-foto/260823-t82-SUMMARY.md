---
quick_id: 260823-t82
slug: fase-1-buckets-privados-lecturas-de-foto
date: 2026-08-23
status: complete
commit: 70b98e6
branch: fix/private-buckets-phase1-reads
---

# Quick Task 260823-t82 — Summary

## Qué se hizo (Fase 1 de 4)

Todas las **lecturas** de fotos/firmas ahora resuelven **signed URLs** en vez de
usar la URL pública directa, para los buckets `evidencias` y `fotos-rostro`.
No se privaron los buckets ni se tocaron las escrituras (fases 2-4). Cero rotura:
las signed URLs funcionan también con bucket público, con fallback a la URL
original si algo falla.

Archivos:
- `lib/services/storage_url_service.dart` (nuevo): `StorageUrlService.signedUrl()`
  y `extractPath()` — normaliza URL pública legacy / firmada / path.
- `lib/worker_detail_page.dart`: foto de evidencia y firma vía `FutureBuilder`;
  PDF del comprobante resuelve signed URL antes de `networkImage`.
- `dashboard/index.html`: helpers `extractStoragePath`, `signedStorageUrl`,
  `hydrateStorageMedia`, `abrirMediaFirmada`; migrados 7 puntos con
  `data-storage-value` + hidratación, más el comprobante (resuelve antes de
  construir el doc), la foto de perfil en detalle de asistencia y el preview
  de `openMantFoto`.

## Verificación

- `flutter analyze` sobre los archivos tocados: sin issues.
- `node --check` del script del dashboard: sintaxis OK.
- Sin `img src` / `a href` / `window.open` con URL cruda de storage restantes.

## Compatibilidad

- Datos legacy (URL pública completa guardada en la BD) siguen funcionando: el
  helper extrae el path y lo firma.
- Bucket `asistencias-fotos` intacto (ya usaba signed URLs).

## Siguientes fases

- **Fase 2:** escrituras → guardar *path* (app: new_delivery_page, sync_service;
  RPCs insert_entrega_offline/online que hoy construyen la URL pública).
- **Fase 3:** backfill de `evidencia_foto_url` / `firma_url` / `foto_rostro_url`
  (URL → path).
- **Fase 4:** privar ambos buckets — SOLO tras confirmar que la app actualizada
  está desplegada en los dispositivos de supervisores.

## Commits

- `70b98e6` — feat(security): fase 1 buckets privados — lecturas vía signed URLs
