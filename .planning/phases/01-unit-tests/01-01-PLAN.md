---
phase: 01-unit-tests
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - lib/services/stock_calculator.dart
  - lib/new_delivery_page.dart
  - test/unit/hash_chain_test.dart
  - test/unit/stock_calculator_test.dart
  - test/unit/offline_queue_test.dart
  - pubspec.yaml
autonomous: true
requirements:
  - UTL-01
  - UTL-02
  - UTL-03
  - UTL-04
  - UTL-05

must_haves:
  truths:
    - "flutter test test/unit/ pasa en menos de 30 segundos sin red ni Supabase"
    - "Un prev_hash incorrecto produce un hash diferente al encadenado correctamente (UTL-01)"
    - "computeStock(ENTRADA - SALIDA) retorna la diferencia aritmética correcta por epp_id (UTL-02)"
    - "validateCart retorna el epp_id fallido cuando cantidad > disponible; null cuando es válido (UTL-03)"
    - "listPending excluye entregas ERROR cuyo nextRetryAt es futuro (UTL-04)"
    - "listPending ordena las entregas pendientes por createdAtClientIso ascendente (UTL-05)"
  artifacts:
    - path: lib/services/stock_calculator.dart
      provides: "Pure static StockCalculator with computeStock and validateCart"
      exports: [StockCalculator]
    - path: test/unit/hash_chain_test.dart
      provides: "UTL-01 hash chain tests"
      contains: "hash chain"
    - path: test/unit/stock_calculator_test.dart
      provides: "UTL-02 and UTL-03 stock tests"
      contains: "StockCalculator"
    - path: test/unit/offline_queue_test.dart
      provides: "UTL-04 and UTL-05 Hive-backed listPending tests (extended)"
      contains: "listPending"
  key_links:
    - from: lib/new_delivery_page.dart
      to: lib/services/stock_calculator.dart
      via: "_cargarStock delegates to StockCalculator.computeStock; submit guard delegates to StockCalculator.validateCart"
      pattern: "StockCalculator\\.computeStock|StockCalculator\\.validateCart"
    - from: test/unit/offline_queue_test.dart
      to: lib/services/offline_queue_service.dart
      via: "hive_test setUpTestHive + OfflineQueueService.init() before calling listPending"
      pattern: "setUpTestHive|OfflineQueueService\\.init"
---

<objective>
Five unit requirements (UTL-01 through UTL-05) covering the three critical business-logic areas of TrazApp: hash chain integrity, stock calculation/validation, and offline queue filtering/ordering.

Purpose: Establish a fast regression net (< 30 s, no network) for the code paths most likely to have legal consequences — tamper-evident hash chains and stock guard logic — and for the offline queue filtering that controls which deliveries are retried.

Output:
- lib/services/stock_calculator.dart — new pure static class extracted from _NewDeliveryPageState
- lib/new_delivery_page.dart — updated to delegate to StockCalculator
- test/unit/hash_chain_test.dart — new, covers UTL-01
- test/unit/stock_calculator_test.dart — new, covers UTL-02 and UTL-03
- test/unit/offline_queue_test.dart — extended with Hive-backed groups, covers UTL-04 and UTL-05
- pubspec.yaml — hive_test added to dev_dependencies
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-unit-tests/RESEARCH.md
@.planning/codebase/STACK.md
@.planning/codebase/CONVENTIONS.md
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Extract StockCalculator and add hive_test dependency (per UTL-02, UTL-03)</name>
  <files>
    lib/services/stock_calculator.dart,
    lib/new_delivery_page.dart,
    pubspec.yaml
  </files>
  <behavior>
    - StockCalculator.computeStock([]) returns {}
    - StockCalculator.computeStock([{epp_id:'A', tipo:'ENTRADA', cantidad:10}, {epp_id:'A', tipo:'SALIDA', cantidad:3}]) returns {'A': 7}
    - StockCalculator.computeStock with two different epp_ids tracks each independently
    - StockCalculator.validateCart({'A': 3}, {'A': 2}) returns 'A' (insufficient stock)
    - StockCalculator.validateCart({'A': 2}, {'A': 2}) returns null (exact stock — allowed)
    - StockCalculator.validateCart({'A': 1}, {'A': 2}) returns null (stock sufficient)
    - StockCalculator.validateCart for epp_id not in stock map uses 0 as disponible and returns that epp_id
  </behavior>
  <action>
    1. Run `dart pub add --dev hive_test` from the project root to add hive_test 1.0.1 to dev_dependencies in pubspec.yaml. This is the only new package needed for the full plan. If pub resolves a different version than 1.0.1, note it but proceed — compatibility with hive 2.2.3 is confirmed.

    2. Create lib/services/stock_calculator.dart with a single class StockCalculator containing exactly two public static methods:

       computeStock(List&lt;Map&lt;String, dynamic&gt;&gt; rows) → Map&lt;String, int&gt;
         Iterates over rows. For each row, reads epp_id (String), tipo (String: 'ENTRADA' or 'SALIDA'), and cantidad ((r['cantidad'] as num).toInt()). Accumulates: mapa[id] = (mapa[id] ?? 0) + (tipo == 'ENTRADA' ? qty : -qty). Returns mapa. This is the exact arithmetic from lib/new_delivery_page.dart lines 181–187.

       validateCart(Map&lt;String, int&gt; carrito, Map&lt;String, int&gt; stockDisponible) → String?
         Iterates carrito.entries. For each entry, reads disponible = stockDisponible[entry.key] ?? 0. If entry.value > disponible, immediately returns entry.key (the failing epp_id as a String). Returns null if all items pass. This matches the guard logic at new_delivery_page.dart lines 973–985, but returns the epp_id rather than constructing a UI error string — the UI layer keeps its name-lookup and error formatting.

       Add a /// doc comment to the class and both methods following the existing convention (Spanish, brief, one-liner). No imports needed beyond dart:core.

    3. Update lib/new_delivery_page.dart:
       - Add import at the top import section (after package imports, before relative imports per CONVENTIONS.md): import 'services/stock_calculator.dart';
       - In _cargarStock (lines ~181–187), replace the inline arithmetic loop with: final mapa = StockCalculator.computeStock(rows.cast&lt;Map&lt;String, dynamic&gt;&gt;());
       - In the submit guard (lines ~973–985), replace the inner for-loop that checks cantidad > disponible with a call to StockCalculator.validateCart(itemsMap, stockDisponible). itemsMap is built by converting the items list to Map&lt;String, int&gt; — map each item as {item['epp_id']: (item['cantidad'] as num).toInt()}. If validateCart returns a non-null eppId, look up the nombre from epps using the same firstWhere pattern already in that block, then call setState(() => error = 'Stock insuficiente: "$nombre" tiene ${stockDisponible[eppId] ?? 0} unidad(es) disponible(s) en bodega.') and return.
       - Do NOT change any other logic, state, or widget structure in new_delivery_page.dart.

    Verify the refactor preserves behavior: the stock display in the UI is unchanged, and the error message text is identical to the original.
  </action>
  <verify>
    <automated>flutter analyze lib/services/stock_calculator.dart lib/new_delivery_page.dart &amp;&amp; flutter test test/unit/stock_calculator_test.dart --no-pub</automated>
  </verify>
  <done>
    - lib/services/stock_calculator.dart exists with StockCalculator class
    - StockCalculator.computeStock and StockCalculator.validateCart are public static methods
    - lib/new_delivery_page.dart imports stock_calculator.dart and delegates both operations
    - pubspec.yaml dev_dependencies contains hive_test
    - flutter analyze reports no errors on the two modified files
    - stock_calculator_test.dart tests pass (written in Task 2, but this task's code must satisfy them)
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Write unit tests — hash chain (UTL-01) and stock calculator (UTL-02, UTL-03)</name>
  <files>
    test/unit/hash_chain_test.dart,
    test/unit/stock_calculator_test.dart
  </files>
  <behavior>
    Hash chain (UTL-01):
    - hashString('|$canonicalPayload1') produces a deterministic non-empty string (baseline hash)
    - hashString('$hash1|$canonicalPayload2') produces hash2 different from hash1
    - hashString('wrong_prev|$canonicalPayload2') produces a value different from hash2 — confirms that changing prev_hash breaks the chain
    - canonicalJson sorts keys alphabetically before encoding — {'z':1,'a':2} encodes as {"a":2,"z":1}

    Stock calculator (UTL-02):
    - computeStock with single ENTRADA returns positive stock
    - computeStock with ENTRADA then SALIDA returns difference (e.g. 10 - 3 = 7)
    - computeStock with two epp_ids tracks each independently
    - computeStock with empty list returns {}

    Stock calculator (UTL-03):
    - validateCart({'casco-001': 3}, {'casco-001': 2}) returns 'casco-001'
    - validateCart({'casco-001': 2}, {'casco-001': 2}) returns null (exact stock is allowed)
    - validateCart({'casco-001': 1}, {'casco-001': 5}) returns null
    - validateCart({'unknown-epp': 1}, {}) returns 'unknown-epp' (no stock = 0)
  </behavior>
  <action>
    Create test/unit/hash_chain_test.dart:

    Import dart:convert for jsonEncode. Import package:crypto/crypto.dart indirectly via package:epp_app/evidence_service.dart. Import package:flutter_test/flutter_test.dart.

    Define a top-level helper that reimplements the canonical JSON logic inline (do NOT import SyncService — keep its internals private):
      String _canonicalJson(Map&lt;String, dynamic&gt; m) — sort keys alphabetically, build ordered map, return jsonEncode(out). This is the exact 5-line pattern from sync_service.dart lines 314–321.

    In main(), add group 'Hash chain — UTL-01' with:
      test 'canonicalJson ordena claves alfabéticamente': build {'z':1,'a':2}, assert result equals '{"a":2,"z":1}'.
      test 'hash de primera entrega es determinístico': compute h1 = EvidenceService.hashString('|${_canonicalJson(payload1)}') twice, assert both equal.
      test 'hash chain: hash2 usa hash1 como prev_hash': compute h1 from payload1 with empty prev_hash prefix, compute expected_h2 = EvidenceService.hashString('$h1|${_canonicalJson(payload2)}'), assert expected_h2 != h1.
      test 'prev_hash incorrecto produce hash diferente': compute actual_h2 = EvidenceService.hashString('$h1|${_canonicalJson(payload2)}'), compute wrong_h2 = EvidenceService.hashString('hash-incorrecto|${_canonicalJson(payload2)}'), assert wrong_h2 != actual_h2.

    Use minimal payloads — only the fields needed to make the test meaningful. Suggested payload structure: {'device_id': 'device-test', 'local_event_id': 'evt-001', 'obra_id': 'obra-001'}. Payloads for payload1 and payload2 differ only in local_event_id to simulate sequential deliveries.

    Create test/unit/stock_calculator_test.dart:

    Import package:flutter_test/flutter_test.dart and package:epp_app/services/stock_calculator.dart.

    In main(), add:
      group 'StockCalculator.computeStock — UTL-02' with the four behavior cases above.
      group 'StockCalculator.validateCart — UTL-03' with the four behavior cases above.

    Use plain Dart literals for test data — no fixtures, no setup, no helper functions needed. Each test is fully self-contained in 3–5 lines.
  </action>
  <verify>
    <automated>flutter test test/unit/hash_chain_test.dart test/unit/stock_calculator_test.dart --no-pub</automated>
  </verify>
  <done>
    - test/unit/hash_chain_test.dart exists with group 'Hash chain — UTL-01' containing 4 tests, all green
    - test/unit/stock_calculator_test.dart exists with groups for UTL-02 and UTL-03 containing 4+4 tests, all green
    - No network calls, no Flutter binding, no Supabase initialization in either file
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: Extend offline_queue_test.dart with Hive-backed listPending tests (per UTL-04, UTL-05)</name>
  <files>
    test/unit/offline_queue_test.dart
  </files>
  <behavior>
    UTL-04 — backoff filter:
    - An ERROR entrega with nextRetryAt one hour from now is NOT returned by listPending
    - An ERROR entrega with nextRetryAt one hour in the past IS returned by listPending
    - A PENDING entrega with no nextRetryAt IS returned by listPending (regression: existing PENDING behavior still works)

    UTL-05 — chronological ordering:
    - Two entregas enqueued with createdAtClientIso B > A are returned by listPending in order [A, B] regardless of enqueue order
  </behavior>
  <action>
    Edit test/unit/offline_queue_test.dart. Append two new groups after the existing groups. Do NOT modify existing tests or the existing _entrega() factory at the top of the file.

    Add these two imports at the top of the file (after existing imports):
      import 'package:hive_test/hive_test.dart';

    Update the existing _entrega() factory to add a localEventId named parameter with a default of 'evt-test-001' and a createdAt named parameter (mapped to createdAtClientIso) with a default of '2026-06-01T10:00:00.000Z'. The factory signature becomes:
      OfflineEntrega _entrega({String localEventId = 'evt-test-001', String createdAt = '2026-06-01T10:00:00.000Z', String status = 'PENDING', int attempts = 0, int maxAttempts = 5, String? nextRetryAt})
    Update the OfflineEntrega constructor call inside _entrega to use localEventId: localEventId and createdAtClientIso: createdAt. All existing tests already pass all parameters positionally via named args so this is backward-compatible.

    Add group 'OfflineQueueService.listPending — backoff filter (UTL-04)':
      setUp: await setUpTestHive(); await OfflineQueueService.init();
      tearDown: await tearDownTestHive();
      test 'ERROR con nextRetryAt futuro es excluido': enqueue one entrega with status: 'ERROR', nextRetryAt: DateTime.now().add(const Duration(hours: 1)).toIso8601String(). Assert listPending() is empty.
      test 'ERROR con nextRetryAt pasado aparece en listPending': enqueue one entrega with status: 'ERROR', nextRetryAt: DateTime.now().subtract(const Duration(hours: 1)).toIso8601String(). Assert listPending() has length 1.
      test 'PENDING sin nextRetryAt aparece en listPending': enqueue one entrega with status: 'PENDING'. Assert listPending() has length 1.

    Add group 'OfflineQueueService.listPending — orden cronológico (UTL-05)':
      setUp: await setUpTestHive(); await OfflineQueueService.init();
      tearDown: await tearDownTestHive();
      test 'ordena por createdAtClientIso ascendente': enqueue entrega with localEventId: 'evt-B', createdAt: '2026-06-01T12:00:00.000Z', then enqueue entrega with localEventId: 'evt-A', createdAt: '2026-06-01T10:00:00.000Z'. Assert listPending()[0].localEventId equals 'evt-A' and listPending()[1].localEventId equals 'evt-B'. Note: call listPending() once and store result, not twice.

    Critical: each test group has its own setUp/tearDown pair. Do not share state across groups. The Hive box is closed and the temp directory cleaned between groups via tearDownTestHive().
  </action>
  <verify>
    <automated>flutter test test/unit/offline_queue_test.dart --no-pub</automated>
  </verify>
  <done>
    - test/unit/offline_queue_test.dart passes all existing tests (isFailed, isPermanentlyFailed, serialization round-trip) unchanged
    - Two new groups added: backoff filter (3 tests, UTL-04) and chronological ordering (1 test, UTL-05)
    - All new tests green with no HiveError exceptions
  </done>
</task>

<task type="auto">
  <name>Task 4: Full suite verification — flutter test test/unit/</name>
  <files></files>
  <action>
    Run the complete unit test suite: flutter test test/unit/ --no-pub

    Expected result: all tests pass including:
    - Existing tests: offline_queue (8 tests), perfil_usuario (11 tests), and any other existing unit tests
    - New tests: hash_chain (4 tests), stock_calculator (8 tests), offline_queue listPending groups (4 new tests)
    - Total: all tests green, no skipped

    If flutter test fails with 'hive_test: package not found' or a version conflict:
      Fallback for Hive initialization — replace setUpTestHive()/tearDownTestHive() in offline_queue_test.dart with manual setup:
        setUp: { final dir = Directory.systemTemp.createTempSync('hive_test_'); Hive.init(dir.path); await OfflineQueueService.init(); }
        tearDown: { await Hive.close(); }
      This fallback is behavior-identical to hive_test and requires no additional dependency.

    Run flutter analyze after all tests pass to confirm no static warnings were introduced.
  </action>
  <verify>
    <automated>flutter test test/unit/ --no-pub &amp;&amp; flutter analyze lib/services/stock_calculator.dart lib/new_delivery_page.dart test/unit/</automated>
  </verify>
  <done>
    - flutter test test/unit/ exits 0 with all tests passing
    - No test output contains 'HiveError', 'LateInitializationError', or 'SocketException'
    - flutter analyze reports no errors or warnings on the modified files
    - Total test run completes in under 30 seconds
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test data → StockCalculator | Crafted in-test; no external input crosses this boundary |
| Test data → OfflineQueueService | Enqueued via test factory; Hive operates on a temp directory isolated from production data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | Hash chain (UTL-01) | mitigate | Test explicitly verifies that wrong prevHash produces a different hash — documents the tamper-evidence property and will catch any future regression that weakens it |
| T-01-02 | Tampering | StockCalculator.validateCart (UTL-03) | mitigate | Test verifies client-side guard blocks over-delivery; server-side trigger trg_prevent_stock_negativo is the defense-in-depth layer (Phase 2) |
| T-01-03 | Tampering | npm/pip/cargo installs | accept | Only one new package (hive_test) added via dart pub add; verified on pub.dev with established publisher history (Netsells); no SLOP or SUS verdict |
| T-01-SC | Tampering | dart pub add --dev hive_test | mitigate | Package legitimacy confirmed in RESEARCH.md Package Legitimacy Audit table — approved, no blocking checkpoint required |
</threat_model>

<verification>
All five requirements verified by automated test commands:

| Requirement | Test File | Command |
|-------------|-----------|---------|
| UTL-01 | test/unit/hash_chain_test.dart | flutter test test/unit/hash_chain_test.dart --no-pub |
| UTL-02 | test/unit/stock_calculator_test.dart | flutter test test/unit/stock_calculator_test.dart --no-pub |
| UTL-03 | test/unit/stock_calculator_test.dart | flutter test test/unit/stock_calculator_test.dart --no-pub |
| UTL-04 | test/unit/offline_queue_test.dart | flutter test test/unit/offline_queue_test.dart --no-pub |
| UTL-05 | test/unit/offline_queue_test.dart | flutter test test/unit/offline_queue_test.dart --no-pub |

Full suite gate: flutter test test/unit/ --no-pub passes in under 30 seconds.
</verification>

<success_criteria>
Phase 1 is complete when ALL of the following are true:

1. flutter test test/unit/ exits 0 with every test green — no failures, no skips
2. A delivery computed with the wrong prev_hash produces a different hash (UTL-01 test fails if this invariant breaks)
3. computeStock([ENTRADA:10, SALIDA:3]) returns {epp_id: 7} (UTL-02)
4. validateCart({epp_id: 3}, {epp_id: 2}) returns the failing epp_id; validateCart({epp_id: 2}, {epp_id: 2}) returns null (UTL-03)
5. listPending() returns [] when the only enqueued item has status ERROR and nextRetryAt one hour in the future (UTL-04)
6. listPending() returns items sorted by createdAtClientIso ascending regardless of enqueue order (UTL-05)
7. flutter analyze lib/services/stock_calculator.dart lib/new_delivery_page.dart reports no errors
8. Total test run under 30 seconds with no network calls
</success_criteria>

<output>
Create .planning/phases/01-unit-tests/01-01-SUMMARY.md when done, using the template at $HOME/.claude/get-shit-done/templates/summary.md.
</output>
