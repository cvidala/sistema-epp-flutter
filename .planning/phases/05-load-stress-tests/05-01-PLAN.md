---
phase: 05-load-stress-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/stress/offline_queue_stress_test.dart
autonomous: true
requirements: [STR-01, STR-02, STR-03]
must_haves:
  truths:
    - "Encolar 200 entregas y llamar listAll() devuelve exactamente 200 registros sin duplicados (STR-01)"
    - "20 enqueues simultáneos via Future.wait() resultan en exactamente 20 registros sin localEventId duplicados (STR-02)"
    - "Con 50 items (30 PENDING + 15 ERROR-future + 5 ERROR-past), listPending() devuelve exactamente 35 en orden cronológico (STR-03)"
    - "flutter test test/stress/ --tags stress pasa sin conexión a Supabase ni red"
  artifacts:
    - path: "test/stress/offline_queue_stress_test.dart"
      provides: "Suite de 3 stress tests sobre OfflineQueueService (volumen, concurrencia, filtro backoff)"
      contains: "@Tags(['stress'])"
      min_lines: 80
  key_links:
    - from: "test/stress/offline_queue_stress_test.dart"
      to: "lib/services/offline_queue_service.dart"
      via: "import package:epp_app/services/offline_queue_service.dart"
      pattern: "package:epp_app/services/offline_queue_service.dart"
    - from: "test/stress/offline_queue_stress_test.dart"
      to: "hive_test lifecycle"
      via: "setUpTestHive / tearDownTestHive en cada group"
      pattern: "setUpTestHive"
---

<objective>
Crear `test/stress/offline_queue_stress_test.dart`: una suite de 3 stress tests que verifican que la cola offline Hive (`OfflineQueueService`) aguanta volumen alto (200 items), concurrencia (20 enqueues simultáneos) y filtrado de backoff bajo carga (50 items), sin corromper el estado del box ni perder registros.

Purpose: Detectar regresiones en la integridad de la cola offline antes de que lleguen a producción. La cola es el corazón del diseño offline-first: si pierde o duplica registros bajo carga, se pierden entregas EPP de cumplimiento normativo.

Output: Un único archivo de test con `@Tags(['stress'])` que cubre STR-01, STR-02 y STR-03, ejecutable con `flutter test test/stress/ --tags stress` sin dependencias de red.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-load-stress-tests/05-CONTEXT.md

@lib/services/offline_queue_service.dart
@test/unit/offline_queue_test.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Suite de stress tests de volumen y concurrencia (STR-01, STR-02)</name>
  <files>test/stress/offline_queue_stress_test.dart</files>
  <read_first>
    - lib/services/offline_queue_service.dart — firma exacta de `OfflineEntrega` (campos `required`), `enqueue()`, `listAll()`, `init()`. El box es `outbox_entregas` y la clave de Hive es `localEventId` (ver `box.put(e.localEventId, ...)`). Esto significa que dos entregas con el mismo `localEventId` SE SOBREESCRIBEN — por eso STR-01 y STR-02 deben usar IDs únicos.
    - test/unit/offline_queue_test.dart — patrón del factory `_entrega()` (líneas 5-27) y patrón de lifecycle `hive_test` (`setUpTestHive()` en setUp, `tearDownTestHive()` en tearDown). Reutilizar ambos exactamente.
  </read_first>
  <behavior>
    - STR-01 (volumen): tras encolar 200 entregas con IDs `'evt-$i'` (i de 0 a 199), `listAll().length == 200`.
    - STR-01 (unicidad): `listAll().map((e) => e.localEventId).toSet().length == 200` (sin colisiones de clave Hive).
    - STR-02 (concurrencia): tras `Future.wait(List.generate(20, ...))` con IDs `'evt-conc-$i'`, `listAll().length == 20`.
    - STR-02 (no duplicados): `listAll().map((e) => e.localEventId).toSet().length == 20`.
  </behavior>
  <action>
    Crear el directorio `test/stress/` y el archivo `offline_queue_stress_test.dart`.

    En la PRIMERA línea del archivo (antes de cualquier import) declarar la anotación de tags con valor `stress` (sintaxis `@Tags(['stress'])`) — per D-04. Esto habilita el filtrado `--tags stress` en CI.

    Importar `package:flutter_test/flutter_test.dart`, `package:hive_test/hive_test.dart`, y `package:epp_app/services/offline_queue_service.dart` (el nombre del paquete es `epp_app`, confirmado en pubspec.yaml).

    Copiar el factory helper `_entrega(...)` desde `test/unit/offline_queue_test.dart` (líneas 5-27) al tope del archivo de stress, con los mismos parámetros nombrados (`localEventId`, `createdAt`, `status`, `attempts`, `maxAttempts`, `nextRetryAt`). No importar el helper desde el archivo unit (no es exportable); copiarlo per la discreción del autor en D-05.

    Crear `group('STR-01 — volumen 200 items')` con `setUp` que llame `await setUpTestHive()` luego `await OfflineQueueService.init()`, y `tearDown` que llame `await tearDownTestHive()` — per D-05.

    Dentro de STR-01, un `test()` que encola 200 entregas en un loop secuencial `for (var i = 0; i < 200; i++)` usando `await OfflineQueueService.enqueue(_entrega(localEventId: 'evt-$i', createdAt: ...))` — per D-06. Espaciar `createdAtClientIso` con incrementos secuenciales (discreción del autor: usar una base ISO fija más `Duration(seconds: i)` o un sufijo numérico ordenable). Luego verificar `listAll().length == 200` y la unicidad vía `Set` de `localEventId` (longitud 200).

    Crear `group('STR-02 — concurrencia 20 enqueues')` con su PROPIO `setUp`/`tearDown` (`setUpTestHive`/`tearDownTestHive`) para aislamiento de box entre groups.

    Dentro de STR-02, un `test()` que ejecute `await Future.wait(List.generate(20, (i) => OfflineQueueService.enqueue(_entrega(localEventId: 'evt-conc-$i', createdAt: ...))))` — per D-07. Luego verificar `listAll().length == 20` y que `listAll().map((e) => e.localEventId).toSet().length == 20` (sin IDs duplicados).

    No usar `OfflineQueueService.newLocalEventId()` (UUID) en estos tests — usar IDs deterministas per D-06/D-07. No agregar anotación `@Timeout` per D-02.
  </action>
  <verify>
    <automated>flutter test test/stress/offline_queue_stress_test.dart --tags stress --plain-name "STR-01"</automated>
    <automated>flutter test test/stress/offline_queue_stress_test.dart --tags stress --plain-name "STR-02"</automated>
  </verify>
  <acceptance_criteria>
    - El archivo `test/stress/offline_queue_stress_test.dart` existe y su primera línea es la anotación `@Tags(['stress'])`.
    - `flutter test test/stress/ --tags stress` ejecuta los tests STR-01 y STR-02 sin variables de entorno ni red.
    - STR-01 afirma `listAll().length == 200` y unicidad de los 200 `localEventId` vía `Set`.
    - STR-02 usa `Future.wait` sobre `List.generate(20, ...)` y afirma `listAll().length == 20` con 20 IDs únicos.
    - `grep -c "setUpTestHive" test/stress/offline_queue_stress_test.dart` devuelve >= 2 (un group por requirement con su propio lifecycle).
  </acceptance_criteria>
  <done>STR-01 y STR-02 pasan: 200 enqueues → 200 registros únicos; 20 enqueues concurrentes → 20 registros únicos. Ambos groups usan lifecycle hive_test aislado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test de filtro de backoff bajo carga (STR-03)</name>
  <files>test/stress/offline_queue_stress_test.dart</files>
  <read_first>
    - lib/services/offline_queue_service.dart — método `listPending()` (líneas 143-167). CRÍTICO: el filtro de backoff (`nextRetryAt` futuro → excluir) SOLO aplica cuando `status == 'ERROR'`. Los items `PENDING` SIEMPRE se incluyen sin importar `nextRetryAt`. `listPending()` también excluye `SENT` y `FAILED`. El resultado se ordena por `createdAtClientIso` ascendente.
    - El factory `_entrega()` ya copiado en Task 1 acepta `status`, `nextRetryAt` y `createdAt` como overrides nombrados — usarlos para construir los 3 tipos de item.
  </read_first>
  <behavior>
    - STR-03 (filtro bajo carga): con 30 PENDING + 15 ERROR-con-nextRetryAt-futuro + 5 ERROR-con-nextRetryAt-pasado (50 items totales), `listPending().length == 35`.
    - STR-03 (composición): los 35 resultados son exactamente los 30 PENDING + los 5 ERROR elegibles; ninguno de los 15 ERROR-futuro aparece.
    - STR-03 (orden): `listPending()` está ordenado por `createdAtClientIso` ascendente (primer elemento es el createdAt más temprano, último el más tardío).
  </behavior>
  <action>
    Agregar al MISMO archivo `test/stress/offline_queue_stress_test.dart` un nuevo `group('STR-03 — filtro backoff bajo carga')` con su propio `setUp` (`setUpTestHive` + `OfflineQueueService.init`) y `tearDown` (`tearDownTestHive`) — aislamiento per D-05.

    Dentro del group, un `test()` que encola exactamente 50 items per D-08:
    - 30 entregas con `status: 'PENDING'`, IDs `'evt-pending-$i'` (i 0..29).
    - 15 entregas con `status: 'ERROR'` y `nextRetryAt` en el FUTURO (usar `DateTime.now().add(const Duration(hours: 1)).toIso8601String()`), IDs `'evt-err-future-$i'` (i 0..14). Estas DEBEN quedar excluidas.
    - 5 entregas con `status: 'ERROR'` y `nextRetryAt` en el PASADO (usar `DateTime.now().subtract(const Duration(hours: 1)).toIso8601String()`), IDs `'evt-err-past-$i'` (i 0..4). Estas DEBEN quedar incluidas.

    Espaciar `createdAtClientIso` de forma secuencial creciente entre los 50 items (discreción del autor) de modo que el orden cronológico sea verificable de forma determinista — los items elegibles deben tener createdAt distinguibles.

    Verificar `OfflineQueueService.listPending().length == 35` (30 PENDING + 5 ERROR-pasado). Verificar adicionalmente que ninguno de los IDs `'evt-err-future-*'` aparece en el resultado (p.ej. comprobar que `listPending().where((e) => e.localEventId.startsWith('evt-err-future')).isEmpty`). Verificar el orden cronológico: que la lista resultante esté ordenada ascendentemente por `createdAtClientIso` (comparar el primer y último elemento, o que la lista sea igual a una copia ordenada de sí misma) per D-08 y el criterio "orden cronológico" del Success Criteria 4.

    No reintroducir items `PENDING` con `nextRetryAt` esperando que se excluyan — el servicio NO filtra PENDING por backoff; solo filtra ERROR. Mantener la composición exacta 30/15/5.
  </action>
  <verify>
    <automated>flutter test test/stress/offline_queue_stress_test.dart --tags stress --plain-name "STR-03"</automated>
  </verify>
  <acceptance_criteria>
    - STR-03 encola exactamente 50 items con la composición 30 PENDING + 15 ERROR-future + 5 ERROR-past.
    - El test afirma `listPending().length == 35`.
    - El test afirma que ningún `localEventId` que empieza con `evt-err-future` aparece en `listPending()`.
    - El test afirma el orden cronológico ascendente por `createdAtClientIso` del resultado de `listPending()`.
    - `flutter test test/stress/ --tags stress` ejecuta los 3 tests (STR-01, STR-02, STR-03) en verde sin red.
  </acceptance_criteria>
  <done>STR-03 pasa: con 50 items en la cola, listPending() devuelve exactamente 35 (excluye los 15 ERROR en backoff) en orden cronológico.</done>
</task>

</tasks>

<verification>
- `flutter test test/stress/ --tags stress` pasa los 3 tests en verde.
- `flutter analyze --no-fatal-infos` no reporta errores nuevos en `test/stress/`.
- El archivo declara `@Tags(['stress'])` en su primera línea.
- Ningún test usa variables de entorno, secrets, ni clientes Supabase (zero dependencias de red).
- Cada `group()` tiene su propio par `setUpTestHive`/`tearDownTestHive`.
</verification>

<success_criteria>
- Encolar 200 entregas → `listAll().length == 200`, 200 IDs únicos (STR-01).
- 20 enqueues via `Future.wait` → `listAll().length == 20`, 20 IDs únicos (STR-02).
- 50 items (30 PENDING + 15 ERROR-future + 5 ERROR-past) → `listPending().length == 35` en orden cronológico (STR-03).
- Los 3 tests corren con `--tags stress` sin red, dentro del presupuesto de tiempo de CI.
</success_criteria>

<output>
Create `.planning/phases/05-load-stress-tests/05-01-SUMMARY.md` when done
</output>
