---
phase: 05-load-stress-tests
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - .github/workflows/test.yml
autonomous: true
requirements: [STR-04]
must_haves:
  truths:
    - "El job `test` en CI ejecuta un paso 'Stress Tests' con `flutter test test/stress/ --tags stress`"
    - "El paso de stress tests está ubicado después de 'Unit & Widget Tests' y antes de 'Integration Tests' (STR-04, D-01)"
    - "El paso de stress tests NO tiene bloque `env:` — confirma cero dependencias de red (STR-04)"
    - "El presupuesto de 60s queda cubierto por el `timeout-minutes: 15` a nivel de job (D-02)"
  artifacts:
    - path: ".github/workflows/test.yml"
      provides: "Paso 'Stress Tests' en el job test que corre la suite de stress con tag filtering"
      contains: "test/stress/ --tags stress"
  key_links:
    - from: ".github/workflows/test.yml — paso Stress Tests"
      to: "test/stress/offline_queue_stress_test.dart"
      via: "flutter test test/stress/ --tags stress"
      pattern: "test/stress/ --tags stress"
---

<objective>
Integrar la suite de stress tests (creada en el plan 05-01) en el pipeline de CI existente: agregar un paso "Stress Tests" en el job `test` de `.github/workflows/test.yml`, ubicado entre "Unit & Widget Tests" e "Integration Tests", sin bloque `env:`.

Purpose: Que cada PR y push a main ejecute automáticamente los stress tests de la cola offline, bloqueando merges si la integridad de la cola se rompe bajo carga. La ausencia de `env:` es la prueba misma de STR-04: los tests no necesitan secrets ni red.

Output: `.github/workflows/test.yml` con un paso nuevo que corre `flutter test test/stress/ --tags stress`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-load-stress-tests/05-CONTEXT.md

@.github/workflows/test.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Agregar paso "Stress Tests" al job de CI (STR-04)</name>
  <files>.github/workflows/test.yml</files>
  <read_first>
    - .github/workflows/test.yml — estructura actual del job `test`. El paso "Unit & Widget Tests" (línea ~34) corre `flutter test test/unit/ test/widget_test.dart --coverage`. El paso "Integration Tests" (línea ~37) tiene un bloque `env:` con todos los secrets de Supabase. El nuevo paso va ENTRE estos dos. El job tiene `timeout-minutes: 15` (cubre el requisito de 60s per D-02).
    - test/stress/offline_queue_stress_test.dart — confirmar que existe y declara `@Tags(['stress'])` (creado en el plan 05-01). El comando de CI depende del tag.
  </read_first>
  <action>
    Editar `.github/workflows/test.yml`. Insertar un nuevo paso llamado "Stress Tests" inmediatamente DESPUÉS del paso "Unit & Widget Tests" y ANTES del paso "Integration Tests" — per D-01.

    El paso debe correr `flutter test test/stress/ --tags stress` — per D-04. NO incluir bloque `env:` en este paso (a diferencia de "Integration Tests") — la ausencia de env es exactamente lo que verifica STR-04: cero dependencias de red ni secrets.

    No agregar un job separado (per D-01: reutilizar el job `test` existente para evitar el overhead de re-setup de Flutter). No tocar `timeout-minutes` (per D-02: el 60s es criterio de éxito, no aserción de runtime; el límite de 15 min del job ya lo cubre). No agregar `--coverage` al paso de stress (los stress tests no son parte del reporte de cobertura del producto). No modificar los pasos "Unit & Widget Tests", "Integration Tests", ni "Upload Coverage".

    Mantener la indentación YAML consistente con los pasos existentes (2 espacios bajo `steps:`, guion + `name:` por paso).
  </action>
  <verify>
    <automated>grep -n "Stress Tests" .github/workflows/test.yml</automated>
    <automated>grep -n "test/stress/ --tags stress" .github/workflows/test.yml</automated>
    <automated>python3 -c "import yaml,sys; d=yaml.safe_load(open('.github/workflows/test.yml')); steps=d['jobs']['test']['steps']; names=[s.get('name') for s in steps]; i=names.index('Unit & Widget Tests'); j=names.index('Stress Tests'); k=names.index('Integration Tests'); assert i < j < k, f'orden incorrecto: {names}'; stress=steps[j]; assert 'env' not in stress, 'el paso Stress Tests NO debe tener bloque env'; assert 'test/stress/ --tags stress' in stress['run'], 'comando incorrecto'; print('OK: orden correcto, sin env, comando correcto')"</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/test.yml` contiene un paso con `name: Stress Tests`.
    - El comando del paso es `flutter test test/stress/ --tags stress`.
    - El paso "Stress Tests" aparece DESPUÉS de "Unit & Widget Tests" y ANTES de "Integration Tests" (verificado por el script Python sobre el YAML parseado).
    - El paso "Stress Tests" NO tiene clave `env:` (verificado por el script Python).
    - El YAML sigue siendo válido y parseable (el script Python falla si no lo es).
    - Los pasos "Integration Tests" y "Upload Coverage" quedan intactos con sus secrets/config originales.
  </acceptance_criteria>
  <done>El job `test` de CI ejecuta los stress tests con `--tags stress` entre los unit tests y los integration tests, sin secrets ni env. STR-04 satisfecho.</done>
</task>

</tasks>

<verification>
- `grep "test/stress/ --tags stress" .github/workflows/test.yml` encuentra el comando.
- El paso "Stress Tests" no contiene bloque `env:`.
- El orden de pasos es: Unit & Widget Tests → Stress Tests → Integration Tests.
- El YAML es válido (parseable por un parser estándar).
</verification>

<success_criteria>
- CI ejecuta los stress tests con tag `--tags stress` en el job `test` existente (STR-04).
- El paso no depende de secrets ni red (sin `env:`), confirmando zero network dependency.
- El presupuesto de 60s queda cubierto por el `timeout-minutes: 15` del job.
</success_criteria>

<output>
Create `.planning/phases/05-load-stress-tests/05-02-SUMMARY.md` when done
</output>
