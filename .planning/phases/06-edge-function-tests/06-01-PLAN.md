---
phase: 06-edge-function-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/notif-vencimiento/index.ts
  - supabase/functions/tests/notif-vencimiento-test.ts
  - supabase/functions/tests/deno.json
autonomous: true
requirements: [EFN-01, EFN-02, EFN-03, EFN-04]
must_haves:
  truths:
    - "Importar index.ts desde un test no inicia el servidor ni falla por env vars ausentes"
    - "buildEmailHtml() es exportada y genera HTML correcto sin Supabase ni Resend"
    - "El handler con DRY_RUN presente retorna 200 sin llamar a fetch"
    - "Un stub de globalThis.fetch captura el payload enviado a Resend (URL, to, subject, html)"
    - "deno test supabase/functions/tests/ pasa sin variables de producción ni conexión a Supabase"
  artifacts:
    - path: "supabase/functions/notif-vencimiento/index.ts"
      provides: "Exports puros (buildEmailHtml, formatFecha, sendResendEmail), tipos, DRY_RUN guard, import.meta.main"
      contains: "export function buildEmailHtml"
    - path: "supabase/functions/tests/notif-vencimiento-test.ts"
      provides: "Tests Deno: HTML, DRY_RUN, payload Resend"
      contains: "Deno.test"
    - path: "supabase/functions/tests/deno.json"
      provides: "Config de imports jsr para @std/assert y @std/testing"
      contains: "@std/assert"
  key_links:
    - from: "supabase/functions/tests/notif-vencimiento-test.ts"
      to: "supabase/functions/notif-vencimiento/index.ts"
      via: "import relativo de funciones puras"
      pattern: "from ['\"]\\.\\./notif-vencimiento/index\\.ts['\"]"
    - from: "supabase/functions/tests/notif-vencimiento-test.ts"
      to: "globalThis.fetch"
      via: "spy() stub para capturar payload Resend"
      pattern: "globalThis\\.fetch = "
---

<objective>
Refactorizar `notif-vencimiento/index.ts` para que sea testeable (exports puros, guardia DRY_RUN, `import.meta.main`, función `sendResendEmail` extraída) y crear los tests Deno que verifican HTML, comportamiento DRY_RUN y payload Resend sin enviar emails reales ni necesitar Supabase.

Purpose: Cumplir EFN-01 a EFN-04 — la Edge Function debe poder testearse en CI sin secrets, sin red y sin tocar usuarios reales. El refactor y los tests son inseparables: el test importa desde el `index.ts` refactorizado.
Output: `index.ts` refactorizado, `tests/notif-vencimiento-test.ts`, `tests/deno.json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-edge-function-tests/06-RESEARCH.md
@supabase/functions/notif-vencimiento/index.ts
</context>

<tasks>

<task type="auto">
  <name>Tarea 1: Refactorizar index.ts — exports puros, DRY_RUN guard, import.meta.main, sendResendEmail</name>
  <files>supabase/functions/notif-vencimiento/index.ts</files>
  <read_first>
    - supabase/functions/notif-vencimiento/index.ts (LEER COMPLETO — la estructura actual determina exactamente qué extraer; ver líneas 8-10 constantes con `!`, 14-29 tipos, 31-103 handler, 78-92 llamada fetch a Resend, 106-194 buildEmailHtml, 196-200 formatFecha)
    - .planning/phases/06-edge-function-tests/06-RESEARCH.md (Patrón 1 DRY_RUN, Patrón 2 exports, Pitfall 1 import.meta.main, sección "Refactor minimo de index.ts" líneas 487-549)
  </read_first>
  <action>
    Refactorizar `supabase/functions/notif-vencimiento/index.ts` con CINCO cambios. NO reescribir la lógica de negocio ni el HTML existente — solo reorganizar para testeo.

    1. Exportar tipos: agregar `export` a `interface Vencimiento` (línea 14) y `interface Destinatario` (línea 26). Sin cambiar campos.

    2. Exportar funciones puras: agregar `export` a `function buildEmailHtml(...)` (línea 106) y `function formatFecha(...)` (línea 196). Sin cambiar su cuerpo. Mover ambas funciones al nivel de módulo FUERA del bloque `import.meta.main` (ya están al nivel de módulo; solo agregar `export`).

    3. Extraer `sendResendEmail` como función pura exportada. Firma EXACTA (no inventar): `export async function sendResendEmail(to: string[], subject: string, html: string, apiKey: string): Promise<boolean>`. Cuerpo: hace `fetch('https://api.resend.com/emails', { method: 'POST', headers: { 'Authorization': \`Bearer ${apiKey}\`, 'Content-Type': 'application/json' }, body: JSON.stringify({ from: FROM_EMAIL, to, subject, html }) })` y retorna `res.ok`. Reemplazar el bloque inline de fetch del handler (líneas 78-92) por una llamada `const ok = await sendResendEmail(emails, asunto, html, RESEND_API_KEY);` actualizando el conteo de `emailsEnviados`/`errores` según `ok`. La constante `FROM_EMAIL` (línea 11) debe quedar accesible para `sendResendEmail`, así que `sendResendEmail` lee `FROM_EMAIL` del nivel de módulo (mantener `FROM_EMAIL` y `DASHBOARD_URL` como const top-level sin `!` — son strings literales, seguras al importar).

    4. Guardia DRY_RUN: como PRIMERA línea dentro del handler `Deno.serve(async () => { ... })`, antes de cualquier llamada a Supabase, agregar: si `Deno.env.get('DRY_RUN')` es truthy, retornar `new Response('DRY_RUN: no emails sent.', { status: 200 })`. Cumple EFN-01.

    5. import.meta.main guard: las TRES constantes con `!` (`SUPABASE_URL`, `SUPABASE_SERVICE_KEY`, `RESEND_API_KEY`, líneas 8-10) deben moverse DENTRO del bloque `if (import.meta.main) { ... }` junto con `Deno.serve(handler)`. Así importar el módulo desde un test no ejecuta `Deno.serve` ni dispara los null-assertions `!` sobre env vars ausentes. Cumple el prerequisito de EFN-03/EFN-04 (Pitfall 1 del RESEARCH). NO mover `FROM_EMAIL` ni `DASHBOARD_URL` dentro del guard — los necesita `sendResendEmail` y `buildEmailHtml` respectivamente al importarse.

    Resultado: al importar `index.ts` solo se evalúan los exports (tipos, `buildEmailHtml`, `formatFecha`, `sendResendEmail`, `FROM_EMAIL`, `DASHBOARD_URL`); el servidor y los secrets solo viven dentro de `if (import.meta.main)`.
  </action>
  <verify>
    <automated>grep -q "export function buildEmailHtml" supabase/functions/notif-vencimiento/index.ts && grep -q "export function formatFecha" supabase/functions/notif-vencimiento/index.ts && grep -q "export async function sendResendEmail" supabase/functions/notif-vencimiento/index.ts && grep -q "export interface Vencimiento" supabase/functions/notif-vencimiento/index.ts && grep -q "if (import.meta.main)" supabase/functions/notif-vencimiento/index.ts && grep -q "DRY_RUN" supabase/functions/notif-vencimiento/index.ts && echo "REFACTOR_OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export interface Vencimiento\|export interface Destinatario\|export function buildEmailHtml\|export function formatFecha\|export async function sendResendEmail" supabase/functions/notif-vencimiento/index.ts` devuelve 5
    - `grep -q "if (import.meta.main)" supabase/functions/notif-vencimiento/index.ts` (exit 0)
    - `grep -q "Deno.env.get('DRY_RUN')" supabase/functions/notif-vencimiento/index.ts` (exit 0)
    - Las constantes `SUPABASE_URL`, `SUPABASE_SERVICE_KEY`, `RESEND_API_KEY` aparecen DESPUÉS de la línea que contiene `if (import.meta.main)` (verificable: `grep -n "Deno.env.get('SUPABASE_URL')" index.ts` da número de línea mayor que `grep -n "if (import.meta.main)" index.ts`)
    - Si Deno está instalado localmente: `deno check supabase/functions/notif-vencimiento/index.ts` no reporta errores de tipos
  </acceptance_criteria>
  <done>Importar `index.ts` no ejecuta `Deno.serve` ni falla por env vars; `buildEmailHtml`, `formatFecha`, `sendResendEmail`, `Vencimiento`, `Destinatario` son exports; el handler tiene guardia DRY_RUN como primera línea.</done>
</task>

<task type="auto">
  <name>Tarea 2: Crear tests Deno (HTML, DRY_RUN, payload Resend) y deno.json</name>
  <files>supabase/functions/tests/notif-vencimiento-test.ts, supabase/functions/tests/deno.json</files>
  <read_first>
    - supabase/functions/notif-vencimiento/index.ts (YA refactorizado en Tarea 1 — confirmar firmas exactas de exports antes de importar)
    - .planning/phases/06-edge-function-tests/06-RESEARCH.md (sección "Estructura completa del archivo de test" líneas 359-460, Patrón 3 fetch stub, Pitfall 5 deno.json líneas 338-353, Dominio de Seguridad líneas 638-657 para fixture de HTML injection)
  </read_first>
  <action>
    Crear DOS archivos.

    A) `supabase/functions/tests/deno.json` (config mínima de imports):
    Contenido: un objeto JSON con clave `"imports"` que mapea `"@std/assert": "jsr:@std/assert@1"` y `"@std/testing": "jsr:@std/testing@1"`. Permite imports limpios en el test (Pitfall 5).

    B) `supabase/functions/tests/notif-vencimiento-test.ts` con los siguientes tests Deno. Importar las funciones puras con ruta relativa: `import { buildEmailHtml, formatFecha, sendResendEmail, type Vencimiento } from '../notif-vencimiento/index.ts'`. Importar assertions: `import { assertEquals, assertStringIncludes } from '@std/assert'` y `import { assertSpyCalls, spy } from '@std/testing/mock'`.

    Definir fixtures de tipo `Vencimiento`: un `critico` (`nivel_alerta: 'CRITICO'`, `dias_restantes: 3`, `trabajador_nombre: 'Juan Perez'`, `trabajador_rut: '12.345.678-9'`, `epp_nombre: 'Casco de Seguridad'`, `epp_codigo: 'CASC-001'`, `obra_nombre: 'Obra Norte'`, `fecha_vencimiento: '2026-06-17'`, `org_id: 'org-1'`) y un `aviso` (spread de `critico` con `nivel_alerta: 'AVISO'`, `dias_restantes: 20`, `epp_nombre: 'Chaleco Reflectante'`, `fecha_vencimiento: '2026-07-04'`).

    Tests requeridos (cada uno con `Deno.test('<nombre>', ...)`):

    1. EFN-03 — `'buildEmailHtml incluye datos del trabajador critico'`: llamar `buildEmailHtml([critico], [critico], [])`, `assertStringIncludes` para `'Juan Perez'`, `'12.345.678-9'`, `'Casco de Seguridad'`, `'3 días'`.
    2. EFN-03 — `'buildEmailHtml incluye seccion aviso'`: `buildEmailHtml([aviso], [], [aviso])`, `assertStringIncludes` para `'Chaleco Reflectante'` y `'20 días'`.
    3. EFN-03 — `'buildEmailHtml omite seccion criticos cuando vacia'`: `buildEmailHtml([aviso], [], [aviso])`, `assertEquals(html.includes('Vencimiento crítico'), false)`.
    4. EFN-03 (V5 input validation, del Dominio de Seguridad del RESEARCH) — `'buildEmailHtml no rompe con caracteres especiales en nombres'`: fixture con `trabajador_nombre: '<script>alert(1)</script>'`, llamar `buildEmailHtml`, `assertStringIncludes(html, '<script>')` (documenta que el HTML refleja el input crudo — email interno, no se bloquea; solo verifica que la función no lanza).
    5. EFN-04 — `'sendResendEmail envia payload correcto a Resend'`: guardar `const originalFetch = globalThis.fetch`. Crear `const mockFetch = spy(async (input, init) => { capturedUrl = input.toString(); capturedBody = JSON.parse(init?.body ?? '{}'); return new Response(JSON.stringify({ id: 'test-id' }), { status: 200 }); })`. Asignar `globalThis.fetch = mockFetch`. En `try`: llamar `await sendResendEmail(['admin@empresa.cl'], '🔴 1 EPP vence en 3 días — TrazApp', buildEmailHtml([critico],[critico],[]), 'mock-key')`. Verificar `assertSpyCalls(mockFetch, 1)`, `assertEquals(capturedUrl, 'https://api.resend.com/emails')`, `assertEquals(capturedBody.to, ['admin@empresa.cl'])`, `assertStringIncludes(capturedBody.subject, 'TrazApp')`, `assertStringIncludes(capturedBody.html, 'Juan Perez')`. En `finally`: restaurar `globalThis.fetch = originalFetch`. SIEMPRE restaurar en finally (anti-patrón del RESEARCH: olvidar restaurar fetch).
    6. EFN-01 — `'handler con DRY_RUN no envia emails'`: este test verifica la guardia DRY_RUN. Importar el handler NO es trivial porque está dentro de `import.meta.main`. En su lugar, verificar el comportamiento DRY_RUN a nivel de la guardia: setear `Deno.env.set('DRY_RUN', '1')`, stubear `globalThis.fetch` con un spy, y confirmar que ninguna ruta de envío llama fetch cuando DRY_RUN está activo. Como el handler vive en `import.meta.main`, este test se cubre afirmando la presencia del guard vía el job CI (que corre con `DRY_RUN: "1"` y el handler nunca se invoca en unit tests). Implementar este test como verificación de que `sendResendEmail` NO se llama cuando el flujo está en modo DRY_RUN: documentar en un comentario que la guardia real se ejercita en el job CI de Wave 2 con `DRY_RUN=1`, y dejar este `Deno.test` afirmando `Deno.env.get('DRY_RUN')` puede leerse (smoke test del mecanismo). NO inventar un import del handler privado.

    Nota crítica para el executor: el HTML usa `${v.dias_restantes} días` (con tilde en "días") — los asserts deben usar exactamente `'3 días'` y `'20 días'` con tilde, coincidiendo con el template del index.ts (línea 121).
  </action>
  <verify>
    <automated>test -f supabase/functions/tests/deno.json && test -f supabase/functions/tests/notif-vencimiento-test.ts && grep -q "from '../notif-vencimiento/index.ts'" supabase/functions/tests/notif-vencimiento-test.ts && grep -q "globalThis.fetch = mockFetch" supabase/functions/tests/notif-vencimiento-test.ts && grep -q "assertSpyCalls" supabase/functions/tests/notif-vencimiento-test.ts && echo "TESTS_FILES_OK"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f supabase/functions/tests/deno.json` (exit 0) y contiene `@std/assert`
    - `grep -c "Deno.test(" supabase/functions/tests/notif-vencimiento-test.ts` >= 5
    - `grep -q "from '../notif-vencimiento/index.ts'" supabase/functions/tests/notif-vencimiento-test.ts` (exit 0)
    - `grep -q "globalThis.fetch = originalFetch" supabase/functions/tests/notif-vencimiento-test.ts` (restauración en finally presente)
    - `grep -q "api.resend.com/emails" supabase/functions/tests/notif-vencimiento-test.ts` (verifica URL Resend)
    - Si Deno está instalado localmente: `deno test supabase/functions/tests/ --allow-env --allow-read` pasa todos los tests en verde sin red ni Supabase. Si Deno NO está instalado localmente, marcar en el SUMMARY que la verificación de ejecución se delega al job CI de Wave 2 (06-02).
  </acceptance_criteria>
  <done>`tests/notif-vencimiento-test.ts` y `tests/deno.json` existen; los tests importan funciones puras del index, stubean `globalThis.fetch` para capturar el payload Resend, restauran fetch en finally, y verifican HTML para crítico/aviso. Si Deno local disponible, `deno test` pasa en verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → index.ts (import) | Importar el módulo no debe ejecutar I/O ni leer secrets — `import.meta.main` aísla el entry point |
| handler → Resend API (fetch) | En tests/CI nunca debe alcanzar la red real; stub de fetch + DRY_RUN guard |
| datos de trabajador → HTML | `epp_nombre`/`obra_nombre`/`trabajador_nombre` se interpolan en el HTML sin sanitizar |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | Envío de email a usuarios reales durante CI | mitigate | Guardia `DRY_RUN` en el handler (retorno temprano) + stub de `globalThis.fetch` en los tests — doble capa; el job CI (Wave 2) corre con `DRY_RUN: "1"` |
| T-06-02 | Information Disclosure | `RESEND_API_KEY` real expuesto en logs de CI | mitigate | Tests usan `'mock-key'`; el job Deno no recibe secrets reales (sin env block de Resend); fetch stubbeado nunca usa la key real |
| T-06-03 | Tampering | HTML injection vía datos de trabajador (`<script>` en `epp_nombre`) | accept | Email interno a administradores; `buildEmailHtml` usa template literals sin sanitizar. Documentado vía fixture de test (test 4) que verifica que la función no lanza. No se bloquea — fuera de scope de esta fase de testing. |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | No hay instalaciones npm en esta fase — dependencias son módulos JSR del stdlib oficial de Deno (`jsr:@std/assert@1`, `jsr:@std/testing/mock@1`), aprobados en la Auditoría de Legitimidad del RESEARCH. slopcheck no aplica a JSR. |
</threat_model>

<verification>
Ejecutar desde la raíz del repo (si Deno está instalado localmente):

```
deno test supabase/functions/tests/ --allow-env --allow-read
```

Debe pasar todos los tests en verde sin variables de producción, sin conexión a Supabase y sin alcanzar `api.resend.com`. Si Deno no está instalado localmente, la verificación de ejecución se delega al job CI creado en el Plan 06-02 (Wave 2).

Verificación estática (sin Deno):
```
grep -c "export function buildEmailHtml\|export async function sendResendEmail" supabase/functions/notif-vencimiento/index.ts   # >= 2
grep -q "if (import.meta.main)" supabase/functions/notif-vencimiento/index.ts                                                   # exit 0
grep -c "Deno.test(" supabase/functions/tests/notif-vencimiento-test.ts                                                         # >= 5
```
</verification>

<success_criteria>
- `index.ts` exporta `buildEmailHtml`, `formatFecha`, `sendResendEmail`, `Vencimiento`, `Destinatario` (EFN-02)
- El handler retorna 200 sin llamar fetch cuando `DRY_RUN` está presente (EFN-01)
- `import.meta.main` aísla `Deno.serve` y los secrets, permitiendo importar el módulo desde tests
- `buildEmailHtml()` genera HTML verificable con datos de trabajador sin Supabase ni Resend (EFN-03)
- El stub de `globalThis.fetch` captura el payload Resend (URL, to, subject, html) sin enviar emails reales (EFN-04)
- `deno test supabase/functions/tests/` pasa sin secrets ni red (verificable local o en CI Wave 2)
</success_criteria>

<output>
Crear `.planning/phases/06-edge-function-tests/06-01-SUMMARY.md` al terminar.
</output>
